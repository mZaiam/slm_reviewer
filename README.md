## Small Language Model (SLM) Agentic Reviewer

This repository implements a simple SLM agent that reviews my personal notes. The model is locally hosted at Oracle, running in ARM CPUs with a total of 24GB VRAM. The agents are implemented using the LangGraph framework.